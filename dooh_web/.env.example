# Client: URL the frontend fetches (use /api/detect for proxy)
VITE_DETECT_URL=/api/detect

# Optional API key
VITE_DETECT_KEY=

# Server: backend for proxy (Vite dev + Vercel API route)
DETECT_API_BACKEND=will change
